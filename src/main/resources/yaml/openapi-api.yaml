openapi: 3.0.3
info:
  title: Hello API
  version: 1.0.0
  description: Simple Spring Boot API (Hello example)

paths:
  /hello:
    get:
      tags:
        - Hello
      summary: Simple hello endpoint
      operationId: getHello
      responses:
        "200":
          description: OK
        "500":
          description: Internal Server Error